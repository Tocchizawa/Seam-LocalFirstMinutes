# GUI (Tauri + React)

このディレクトリはデスクトップアプリのフロントエンドおよび Tauri 側実装です。

## 開発

```bash
pnpm install
pnpm tauri dev
```

## ビルド

```bash
pnpm build
pnpm tauri build --bundles app --ci
```

詳細はリポジトリルートの `README.md` を参照してください。
